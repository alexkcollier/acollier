{
  "name": "acollier",
  "version": "1.0.0",
  "description": "acollier.ca",
  "author": "Alex Collier",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore . && stylelint ./**/*.{vue,scss}",
    "lintfix": "npm run lint -- --fix",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "nuxt": "^2.10.2"
  },
  "devDependencies": {
    "@acollier/eslint-config-vue": "^1.4.0",
    "@acollier/stylelint-config-scss": "^1.1.1",
    "babel-eslint": "^10.0.3",
    "cross-env": "^5.2.1",
    "eslint": "^5.16.0",
    "eslint-loader": "^2.2.1",
    "prettier": "1.14.3",
    "sass": "^1.23.7",
    "sass-loader": "^7.3.1",
    "stylelint": "^10.1.0"
  }
}
