{
  "name": "acollier",
  "version": "1.0.0",
  "description": "acollier.ca",
  "author": "Alex Collier",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore . && stylelint ./**/*.{vue,scss}",
    "lintfix": "npm run lint -- --fix",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "nuxt": "^2.6.3"
  },
  "devDependencies": {
    "@acollier/eslint-config-vue": "^1.3.2",
    "@acollier/stylelint-config-scss": "^1.1.1",
    "babel-eslint": "^10.0.1",
    "cross-env": "^5.2.0",
    "eslint": "^5.16.0",
    "eslint-loader": "^2.1.2",
    "prettier": "1.14.3",
    "sass": "^1.20.1",
    "sass-loader": "^7.1.0",
    "stylelint": "^10.0.1"
  }
}
